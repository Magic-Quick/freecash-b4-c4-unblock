# ASSET_SPEC — «Unblock» (Freecash B4C4)

> Ревизия синхронизирована с `DESIGN_UPDATE_PLAN.md` (Шаг 0.1). Арт-направление сменилось с
> `concept-1` (тёплое дерево) на **candy / pastel «розово-сиреневый мармелад»** — по утверждённому
> макету `.playbox/approved design.png` + нарезке дизайнера `approved assets/` (21 файл).
> Оригиналы нарезки не трогаем; папка лежит **вне** `assets/`, в бандл не попадает.
> Макет 3:4 — референс композиции, **не 1-в-1**: наш формат 720×1280 (9:16), см. `DESIGN_UPDATE_PLAN.md` §0/§1.3.

## Общий стиль (candy)
- Розово-сиреневый мармелад: глянцевые скруглённые формы, мягкий свет, пастельная плашка фона.
- Главный блок — красно-розовый (выезжает как блок, без машинки), препятствия — жёлтые (единый цвет).
- Скругления и глянец — как нарисовано дизайнером в `approved assets/`; для сгенерированных нами
  элементов (пэкшот-панель, CTA-кнопка) держать тот же язык форм.
- Текст на ассетах (цифры, подписи) — шрифт согласован: **Luckiest Guy**, `assets/LuckiestGuy-Regular.ttf` (Luckiest Guy, 58 KB). Все
  `cc.Label` сцены переведены на него (`_isSystemFontUsed=false`, `_isBold=false` — фейковый bold
  поверх и без того жирного дисплейного начертания мылит контур).
- Монеты/FC отменены целиком (решение владельца 0.3) — денежных символов и монетного арта в проекте нет.

## Таблица ассетов — раскладка выполнена (план §4.1)

Все 21 файл из `approved assets/` разложены + 4 запечённых (цвет блоков, см. ниже).

| Назначение | Источник | Прим. |
|------------|----------|-------|
| `art/bg/bg_gameplay.png` | `BG.png` | резкий — фон геймплея (решение 0.10) |
| `art/bg/bg_blur.png` | `BG_blur.png` | размытый — подложка пэкшота / паузы |
| `art/board/board.png` | `Board_V2.png` | **основной**: чистый, без выреза |
| `art/board/board_notched.png` | `Board.png` | ⚠ дефектный (обрезанная правая рамка + артефакт подложки), фолбэк только если шов накладки виден глазами |
| `art/board/exit_notch.png` | `Exit.png` | накладка выреза |
| `art/board/exit_arrows.png` | `Arrows.png` | стрелки отдельно (под пульсацию) |
| `art/blocks/block_len2.png` | `Block2Shadow.png` | серая база (тень запечена, `RGB=0`) |
| `art/blocks/block_len3.png` | `BlockShadow.png` | серая база |
| `art/blocks/block_len{2,3}_{obst,main}.png` | — | ✅ **запечены** (см. «Цвет блоков» ниже) — это то, что идёт в игру |
| `art/blocks/block_len{2,3}_noshadow.png` | `Block{2,}.png` | не используется |
| `art/ui/panel.png` | `UI.png` | перезапись, UUID сохранён |
| `art/ui/star_on.png` | `star.png` | перезапись, UUID сохранён |
| `art/ui/star_off.png` | `StarBG.png` | перезапись, UUID сохранён |
| `art/ui/btn_pause.png` | `button4.png` | |
| `art/ui/btn_hint.png` | `button3.png` | лампа + пустой badge под цифру |
| `art/ui/btn_undo.png` | `button2.png` | |
| `art/ui/btn_restart.png` | `button1.png` | |
| `art/ui/btn_base.png` | `button.png` | пустая кнопка (основа под mute / CTA) |
| `art/ui/chevron_next.png` | `1.png` | круглый шеврон «›», декор без функции (уровень один) |
| `art/ui/lamp.png` | `lamp.png` | лампа отдельно |
| `art/ui/freecash_logo.png` | `Freecash_Stylized_Logo_6.png` | 1896×524 → уменьшить до h≈120 |

## Цвет блоков — запечён в PNG, не тинт (план §3)

`block_len2/3.png` — чистая шкала серого (`meanSat = 0.000`), арт рассчитан на покраску. Один арт →
и препятствия, и главный блок; это убирает `block_main.png` как отдельный файл.

Цвета из макета: препятствие **`#FDCD28`** (253,205,40), главный блок **`#FB5A78`** (251,90,120).

`Sprite.color` **не используется** для базового вида — это умножение, оно только затемняет, а база
арта не белая (медиана 187–197), макетный цвет ярче доступного тинта. Вместо этого цвет запечён
операцией gradient map «чёрный → C → белый» (тени сохраняют `RGB=0`, блики остаются белыми, тело
совпадает с макетом до байта). Скрипт: **`tools/bake_block_colors.py`** — воспроизводимо, полное
обоснование операции в docstring скрипта и в `DESIGN_UPDATE_PLAN.md` §3.

`Sprite.color` остаётся свободен под FX (вспышка на «ход невозможен», затемнение на паузе).

## Блоки — больше не 9-slice

~~Старая политика: `block_tile.png` / `block_main.png` — 9-slice (border 24), тянутся под любую длину.~~
Отменено. Новый арт — **готовые кадры под конкретную длину** (1×2 и 1×3), а не резиновый тайл:

- `Sprite.type = SIMPLE`, `sizeMode = CUSTOM`; растяжения нет вообще.
- 4 запечённых кадра: `len2/len3` × `obst/main`.
- Горизонтальные блоки — поворот вертикального арта на 90° (решение владельца 0.3), отдельного
  горизонтального арта нет.
- То же самое расширение затронуло игровое поле: `board_frame.png` (9-slice border 48) тоже снят —
  сетка 6×6 запечена в цельный `board.png`, поле больше не масштабируемая рамка с отдельными
  `Cell.prefab`, а один спрайт с зафиксированной геометрией (числа — `GameConfig`/`SCENE_SETUP.md`,
  источник — `DESIGN_UPDATE_PLAN.md` §2).

## Чего не хватает (план §4.3)

| # | Ассет | Статус |
|---|-------|--------|
| 1 | Пэкшот: панель + широкая кнопка «Play & Earn» | рисуем сами (решение 0.4) — `panel.png` + `btn_base.png` как основа |
| 2 | Спрайт руки для туториала | владелец запросит; пока `art/ui/finger.png` (болванка) — **не удалять**, сцена ссылается |
| 3 | Иконка звука on/off | место в композиции и ответ отложены до решения по кнопке паузы (`OPEN_ISSUES #9`, план §6 вопрос B/C) |
| 4 | Вспышка на выходе / частицы на победу | GDD §3; пока `art/sprites/spark.png` (болванка) — **не удалять** |
| ~~5~~ | ~~Шрифт для цифр/подписей~~ | ✅ закрыт 2026-08-27: владелец добавил `assets/LuckiestGuy-Regular.ttf` (Luckiest Guy, 58 KB), проставлен всем `cc.Label` |
| 6 | Портретная нарезка фона | `BG*` только landscape; в портрете апскейл ×1.67 + обрезка ~70% по ширине (план §4.4, §6 вопрос E) |

## Правила качества
- PNG RGBA8 без потерь; альфа не квантуется. Фоны (`bg_gameplay`, `bg_blur`) без альфы.
- Оригиналы `approved assets/` не редактируются на месте — все правки идут в копии под `assets/art/**`.
- Никаких `.meta` вручную — Cocos создаёт при импорте (кроме `build_prefab`, который сам пишет `.meta`).
- **Держим тень в отдельном канале, не в цвете.** `*Shadow`-варианты предпочтительны при выборе
  источника: у них запечённая тень имеет `RGB = 0`, поэтому покраска её не красит.
- Вес: сырой арт сейчас ≈3.9 MB при бюджете 5 MB (GDD §10), компрессия — на этапе билда (решение 0.5,
  план §4.5). Отдельно от билда: удалить мёртвые файлы после подтверждения цвета — `board_notched.png`
  (568 KB), `block_*_noshadow.png` (56 KB), серые базы `block_len{2,3}.png` (58 KB); логотип ужать до
  рабочей высоты (~120 px, сейчас 1896×524 / 640 KB).

## Открытые вопросы, влияющие на этот спек
Перенесено из `DESIGN_UPDATE_PLAN.md` §6 — не блокируют текущую раскладку, отслеживаются отдельно:
- ~~**D** — шрифт для цифр/подписей~~ ✅ закрыт: Luckiest Guy, см. выше.
- **E** — портретная нарезка фона не сделана, живём с обрезкой до решения.
- **C** — ассет и место кнопки mute отложены до ответа владельца по кнопке паузы (вопрос B).
