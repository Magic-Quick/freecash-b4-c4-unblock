# OPEN_ISSUES — «Unblock» (Freecash B4C4)

> Открытые вопросы и допущения. Заблокированные пункты НЕ мешают сборке болванок (болванки на placeholder/stub).
> Адресаты: технические — лиду; по GDD — Роме; прочие — Жене.

## Блокеры финала (нужен ответ до финальных билдов)

### #1 Финальные ассеты — Женя/Вика
**Арт-направление утверждено владельцем: `concept-1.png` — классическое дерево (Unblock Me), без машинки,
главный блок = красный, выезжает как блок.** Строим на placeholder-ассетах по `ASSET_SPEC.md` в этом стиле.
→ Осталось: дождаться нарезки финальных ассетов от дизайнера и заменить болванки.

### #2 Клиентский логотип Freecash — Женя/лид
`cocos-asset-maker` ожидает файл логотипа в корне; его нет в проекте. **Допущение:** до получения —
нейтральный текстовый placeholder «FREE CASH». → Прислать актуальный клиентский логотип (PNG, прозрачный).

### #3 Playbox `plbx` adapter / SDK — лид
Глобальный `plbx` (`game_ready/tap/download/game_end`) в проекте не подключён. **Допущение:** `Core/Playbox.ts` —
обёртка с no-op фолбэком (работает в редакторе), реальный SDK инжектится на сборке под сеть.
→ Подтвердить способ подключения и per-network инжект.

## Дизайн-вопросы (влияют на данные/раскладку) — Рома

### #4 Размер поля и раскладки уровней
**Допущение:** сетка 6×6 (стандарт Unblock), 2 уровня, раскладки в `assets/data/levels.json`, выводимые из concept-4
(L1 — простой, L2 — больше блоков, часть с «×»-декором). → Подтвердить размер и точные раскладки, число ходов.

### #5 Разбивка наград
**Допущение:** L1 = +9 FC, L2 = +10 FC, итого 19 FC (GDD: «+9 FC» и «19 FC»). → Подтвердить split.

### #6 HUD-элементы (MOVES/Record/звёзды/паузa/подсказка/undo/restart)
Присутствуют на концептах. **Допущение:** декоративные, без функционала (плейбл ведёт игрока, нет fail-state).
→ Подтвердить, что интерактивность не требуется.

### #7 Landscape-раскладка
GDD §4 требует адаптив под portrait И landscape. **Допущение:** portrait — базовый; landscape — HUD по краям,
поле по центру, адаптер по aspect ratio. → Подтвердить, каким сетям реально нужен landscape (приоритет).

## Технические заметки (не блокеры)
- Звук/музыка — `SoundSystem` = stub; источник (сток/генерация/база) и права на коммерцию — уточнить (GDD §6). Лид.
- Блоки спавнятся из `Block.prefab` по данным уровня (не пред-размещены в сцене) — осознанное решение
  ради data-driven и «минимум хардкода».
- Вес: следить за суммой ассетов (бюджет 5 MB) уже на фазе 4.
